<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Width="378" Height="567">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" Margin="8">
        <StackPanel Width="200">
            <TextBlock Text="Код магазина" FontWeight="SemiBold" />
            <TextBlock Text="{Binding Lfakdnr}" FontSize="16" />
        </StackPanel>
        <StackPanel Margin="8,0,0,0">
            <Image Source="{Binding Tenam, Converter={StaticResource BarcodeConverter}}" Height="60" />
            <TextBlock Text="{Binding Tenam}" HorizontalAlignment="Center" />
        </StackPanel>
    </StackPanel>

    <Separator Grid.Row="1" Margin="8,0" />

    <StackPanel Grid.Row="2" Margin="8">
        <TextBlock Text="Адрес доставки" FontWeight="SemiBold" />
        <TextBlock Text="{Binding Gpbez}" />
        <TextBlock Text="{Binding Gport1}" />
        <TextBlock Text="{Binding Gpstrasse}" />
    </StackPanel>

    <Separator Grid.Row="3" Margin="8,0" />

    <Grid Grid.Row="4" Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel>
            <TextBlock Text="Грузополучатель" FontWeight="SemiBold" />
            <TextBlock Text="{Binding Gpbez}" Margin="0,0,0,6" />

            <TextBlock Text="Дополнительные сведения" FontWeight="SemiBold" />
            <TextBlock Text="{Binding Bstchgnam5}" TextWrapping="Wrap" Margin="0,0,0,6" />

            <TextBlock Text="Вес брутто (кг)" FontWeight="SemiBold" />
            <TextBlock Text="{Binding Brutto}" />
        </StackPanel>

        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
            <TextBlock Text="EAC" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" />
            <TextBlock Text="Коробов в заказе" Margin="0,12,0,0" />
            <TextBlock Text="{Binding Countbst}" FontSize="16" HorizontalAlignment="Center" />
            <TextBlock Text="Изделий в коробе" Margin="0,12,0,0" />
            <TextBlock Text="{Binding SumBst}" FontSize="16" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Grid>
